export default function CatalogPage() {
  // TODO: fetch products via AppSync listProducts (API key auth), grid of cards
  return (
    <main className="mx-auto max-w-6xl px-4 py-12">
      <h1 className="text-3xl font-bold text-leaf">Our Collection</h1>
      <p className="mt-2 text-gray-600">Garlands, veni, pooja sets, and decorations.</p>
    </main>
  );
}
